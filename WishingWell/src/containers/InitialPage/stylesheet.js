'use strict';

import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  inputs: {
    width: '80%'
  },
  container: {
    display: 'flex',
    alignItems: 'center',
    width: '100%'
  }
});