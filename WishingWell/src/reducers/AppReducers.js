
/*jshint esversion: 6*/

const initialState = {
  loggedIn: false,
};

const appReducers = (state = initialState, action) => {
  switch (action.type) {

    default:
      return state;
  }
};

export default appReducers;